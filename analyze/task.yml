platform: linux

inputs:
  - name: tasks
  - name: layers
  - name: workspace

outputs:
  - name: layers
  - name: workspace

run:
  path: tasks/analyze/run.sh

params:
  # Image being built (or rebuilt)
  IMAGE_NAME:
