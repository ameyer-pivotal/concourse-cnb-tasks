platform: linux

inputs:
  - name: source
  - name: tasks
  - name: layers
  - name: workspace

outputs:
  - name: layers

run:
  path: tasks/analyze/run.sh

params:
  # Image being built (or rebuilt)
  IMAGE_NAME:

  # User ID of the builder image user
  USER_ID: 1000

  # Group ID of the builder image user
  GROUP_ID: 1000