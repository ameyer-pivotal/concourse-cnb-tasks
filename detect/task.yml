platform: linux

inputs:
  - name: source
  - name: tasks
  - name: layers

outputs:
  - name: layers

run:
  path: tasks/detect/run.sh

params:
  # Subdirectory within source to build (optional)
  SOURCE_SUBDIR:
