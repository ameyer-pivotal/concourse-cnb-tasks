platform: linux
image_resource:
  type: docker-image
  source:
    # Set the builder for this phase
    repository: cloudfoundry/cnb
    tag: bionic

inputs:
  - name: source
  - name: tasks

run:
  path: tasks/detect/run.sh

params:
  # Subdirectory within source to build (optional)
  SOURCE_SUBDIR: